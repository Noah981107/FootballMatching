<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="repository.non_auth.comment.TeamBoardCommentMapper">
    <select id="list" parameterType="string" resultType="domain.Comment">
        SELECT b.name as writer, a.board_id as boardNumber, a.postDate, a. modifiedDate, a.content
        FROM footballMatching.team_board_comment as a
        LEFT JOIN footballMatching.user as b
        ON a.user_idx = b.idx
        WHERE board_id = #{boardNumber}
    </select>
</mapper>