<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="repository.UserMapper">

    <select id="check_id" resultType = "string" parameterType="string">
        SELECT id FROM footballMatching.user WHERE id = #{id};
    </select>

    <select id="check_phoneNumber" resultType = "string" parameterType="string">
        SELECT phoneNumber FROM footballMatching.user WHERE phoneNumber = #{phoneNumber};
    </select>

    <insert id="sign_up" parameterType = "domain.Users">
        INSERT INTO footballMatching.user (id, password, name, phoneNumber, location_code, joinDate)
        VALUES (#{id}, #{password}, #{name}, #{phoneNumber}, #{location_code}, #{joinDate})
    </insert>

</mapper>