<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="repository.BusinessUserMapper">

    <!--아이디 중복 검사-->
    <select id="check_id" resultType = "string" parameterType="string">
        SELECT id FROM footballMatching.business_user WHERE id = #{id};
    </select>

    <!--전화번호 중복 검사-->
    <select id="check_phoneNumber" resultType = "string" parameterType="string">
        SELECT phoneNumber FROM footballMatching.business_user WHERE phoneNumber = #{phoneNumber};
    </select>

    <!--회원 가입-->
    <insert id="sign_up" parameterType="domain.BusinessUsers">
        INSERT INTO footballMatching.business_user (id, password, name, phoneNumber, field_id, joinDate)
        VALUES (#{id}, #{password}, #{name}, #{phoneNumber}, #{field_id}, #{joinDate});
    </insert>
</mapper>